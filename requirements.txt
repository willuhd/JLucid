# JLucid runtime — top-level deps (Apple Silicon venv, CPython 3.14.7).
# Recreate with:  ../.python3.14/install/bin/python3.14 -m venv .venv && .venv/bin/pip install -r requirements.txt
# Code runs with: PYTHONPATH=src python src/leida/make_figures.py
#
# torch is intentionally unpinned: stock PyTorch ships arm64/MPS wheels
# (verified: torch 2.14.0, MPS available). h5py + torchdiffeq are retrain-only
# (scripts/41_train_ode4.py bundle I/O + neural-ODE solvers).
# openneuro-py covers OpenNeuro dataset downloads (ds000030, ds005899).

numpy==2.5.3
scipy==1.18.1
pandas==3.0.5
scikit-learn==1.9.1
statsmodels==0.15.0
matplotlib==3.11.2
nibabel==5.4.2
nilearn==0.14.1
torch
tqdm==4.70.1
h5py==3.16.0
openneuro-py==2026.7.1
torchdiffeq==0.2.5
